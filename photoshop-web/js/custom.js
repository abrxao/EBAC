const controlButtons = document.querySelectorAll('.banner__slide__controlButtons button');
const slide = document.querySelector('.banner__slide__info');

controlButtons.forEach((b)=>{
    b.addEventListener('click', ()=>{
        if(b.getAttribute('id')=='bt1'){
            
        }
        
    });
});